<Window x:Class="Dunia.Toolkit.ResourceReferencesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Resource references"
        Width="900"
        Height="560"
        MinWidth="680"
        MinHeight="380"
        WindowStartupLocation="CenterOwner"
        AutomationProperties.Name="Resource references"
        Background="#101010"
        Foreground="#F2F2F2"
        FontFamily="Segoe UI">
    <Window.Resources>
        <SolidColorBrush x:Key="PanelBrush" Color="#181818" />
        <SolidColorBrush x:Key="ControlBrush" Color="#242424" />
        <SolidColorBrush x:Key="BorderBrush" Color="#3C3C3C" />
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#F2F2F2" />
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource ControlBrush}" />
            <Setter Property="Foreground" Value="#F2F2F2" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="Padding" Value="8,6" />
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="#101010" />
            <Setter Property="Foreground" Value="#F2F2F2" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#2E5D87" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="Padding" Value="8,4" />
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock FontFamily="Bahnschrift SemiBold" FontSize="22" Text="RESOURCE REFERENCES" />
        <TextBlock x:Name="SummaryText" Grid.Row="1" Margin="0,5,0,14" Foreground="#A6A6A6"
                   AutomationProperties.LiveSetting="Polite" />
        <Border Grid.Row="2" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DataGrid x:Name="ReferencesGrid"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      Background="#101010"
                      BorderThickness="0"
                      RowHeaderWidth="0"
                      SelectionMode="Extended"
                      EnableRowVirtualization="True">
                <DataGrid.Resources>
                    <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
                        <Setter Property="AutomationProperties.Name" Value="{Binding AccessibleName}" />
                    </Style>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Width="110" Binding="{Binding Offset}" Header="OFFSET" />
                    <DataGridTextColumn Width="80" Binding="{Binding Endianness}" Header="ORDER" />
                    <DataGridTextColumn Width="170" Binding="{Binding Hash}" Header="CRC64" />
                    <DataGridTextColumn Width="*" Binding="{Binding Name}" Header="RESOURCE" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Window>
